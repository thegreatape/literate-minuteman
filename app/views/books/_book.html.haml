.book-result
  - if book.image_url.present?
    .image.pull-left
      = image_tag book.image_url
  .data
    %h2.title
      = book[:title]
      %span.divider /
      = book[:author]
    %table.table.table-condensed
      %tr
        %th Title
        %th Status
        %th Location
        %th Call Number
      - book.copies.each do |copy|
        %tr{class: ('success' if copy.status.downcase == "available")}
          %td=copy.title
          %td=copy.status
          %td=copy.location.name
          %td=copy.call_number
