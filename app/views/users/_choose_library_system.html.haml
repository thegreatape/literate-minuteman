%h2 Choose your library system
What library systems are you a part of?
= form_tag :controller => :users, :action => :save_library_systems do
  - LibrarySystem.all.each do |system|
    = label_tag "system[#{system.id}]", system.name
    = check_box_tag("system[#{system.id}]")
  = submit_tag "Save"
